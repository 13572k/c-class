#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <math.h>


int main(void)
{
	/*
	----------- 연산자
	+
	-
	/
	%

	<< >>

	==   서로 같다
	!=   서로 다르다
	> < >= <= 부등식
	&&    and (두 조건이 모두 참일 때 참을 반환)
	||    or (두 조건중 하나 이상 참일 때 참을 반환)
	!     not

	----------- 변수

	자료형 변수 이름 = 값;
	int a = 10;
	int a, b, c;   변수의 다중 선언


	----------- 출력함수
	printf("출력값");

	printf("%d", a)
	a에 저장된 값을 출력

	\n 줄넘기기


	----------- 자료형

	int 정수형
	float, double, long 실수형
	char 문자
	string 문자열
	bool, boolean 논리형


	sizeof (자료형); 자료형의 바이트 값을 반환


	// 입력함수
#define _CRT_SECURE_NO_WARNINGS
scanf("출력타입", &변수);

int a;
scanf("%d", &a);
printf("입력된 값" : %d", a);




	------------ 출력타입 (자료형)
	 format type
	 %d 정수값 출력
	 %f 실수값 출력
	 %c 문자 출력타입
	 %s 문자열 출력타입

	 printf("%c", 65) 아스키 코드에 대응하여 값이 출력됨

	 // if, else, else if

	 ------ 반복문

	 for , while
	 // for문의 기본적인 형태
	 for (int i = 0; i < 10; i++)
	 {
	 printf("hello world! &d \n", i);
	 }


	 // 이중 for문
	 for(int i = 0; i < 5; i++)
	 {
	 for (int j = 0; j < 3; j++)
	 {
	 printf(" i : %d \n j : %d \n", i, j);
	 }
	 }



	 while
	 int a = 0;
	 while (a != 20)
	 {
	 a ++
	 printf("while 반복 %d \n");
	 }

	 for 반복 횟수가 정해져 있을때
	 while 반복 조건이 있을 때

	 do
	 {
	 printf("do while 반복 : %d \n", a);
	 a -= 2
	 } while ( a > -10)


	 for (int i = 0; i < 10; i++)
{
	if (i == 4)
	{
		break;
	}
	printf("%d \n", i);
}
printf("\n\n\n");

for (int i = 0; i < 10; i++)
{
	if (i == 6)
	{
		continue; 해당 반복문을 멈추고 재시작
	}
	printf("%d\n", i);
}

for (int i = 0; i < 3; i++)
{
for (int j = 0; j < 4; j ++)
{
if ( j == 2)
{
break;
}
printf("%d, %d \n" i , j);
}
}

---- 배열
자료형 변수이름 [크기]


	*/
	//
	//int a[10];
	//printf("%d", a[0]); // 초기화 하지 않은 값은 쓰레기 값을 가짐
	//
	//for (int i = 0; i < 10; i++)
	//{
	//	printf("%d\n", a[i]);
	//}
	//
	//int ary[10] = { 0 }; // 배열 선언의 단계에서 값을 초기화
	//int ary2[10] = { 1,2,3,4,5,6,7,8 };
	//for (int i = 0; i < 10; i++)
	//{
	//	printf("%d %d \n", ary[i], ary2[i]);
	//}
	//
	//


	//}



// 첫째줄에 입력될 값의 개수 n이 주어진다
// 두번째 줄부터 n개의 정수값이 하나씩 각 줄마다 입력된다.
// 입력된 모든 값을 배열에 담아 출력하는 코드를 작성한다.


//int n;
//int j = 0;
//scanf("%d", &n);
//
//for (int i = 0; i <= n; i++)
//{
//	printf("%d \n", i);
//	j = j + i;
//}
//printf("%d \n", j);


// 입력값 n을 입력받고 이에 따른 소인수 분해값을 배열에 저장하여 출력한다.

int n;
scanf("%d", n);

for (int i = 1; i <= n; i++)
